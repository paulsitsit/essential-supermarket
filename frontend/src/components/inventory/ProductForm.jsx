import { useEffect, useState } from 'react';
import client from '../../api/client';
import { getErrorMessage } from '../../utils/errors';

const empty = { name: '', barcode: '', sku: '', qrCode: '', category: '', supplier: '', brand: '', description: '', unitType: 'piece', branch: 'Main Branch', currentStock: 0, reorderLevel: 10, costPrice: 0, expirationDate: '' };

export default function ProductForm({ initialProduct, categories, suppliers, onSuccess }) {
  const [form, setForm] = useState(initialProduct ? { ...empty, ...initialProduct, category: initialProduct.category?._id || initialProduct.category || '', supplier: initialProduct.supplier?._id || initialProduct.supplier || '' } : empty); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  useEffect(() => { if (!form.qrCode && form.barcode) setForm(current => ({ ...current, qrCode: current.barcode })); }, [form.barcode]);
  function change(key, value) { setForm(current => ({ ...current, [key]: value })); }
  async function submit(event) { event.preventDefault(); setError(''); if (!form.name || !form.sku) return setError('Product name and SKU are required.'); if (Number(form.currentStock) < 0 || Number(form.reorderLevel) < 0 || Number(form.costPrice) < 0) return setError('Quantity, reorder level, and cost price cannot be negative.'); setBusy(true); try { const payload = { ...form, currentStock: Number(form.currentStock), reorderLevel: Number(form.reorderLevel), costPrice: Number(form.costPrice), category: form.category || undefined, supplier: form.supplier || undefined, qrCode: form.qrCode || form.barcode || undefined }; if (initialProduct) await client.put(`/products/${initialProduct._id}`, payload); else await client.post('/products', payload); onSuccess(); } catch (err) { setError(getErrorMessage(err, 'Unable to save product')); } finally { setBusy(false); } }
  return <form className="product-form" onSubmit={submit}>{error && <div className="form-error">{error}</div>}<div className="form-section"><h3>Basic information</h3><div className="form-grid"><label className="span-two">Product name<input required value={form.name} onChange={e => change('name', e.target.value)} placeholder="e.g. Bottled Water 500ml" /></label><label>SKU<input required value={form.sku} onChange={e => change('sku', e.target.value.toUpperCase())} placeholder="BW-500" /></label><label>Barcode <span className="field-hint">Leave blank to generate</span><input value={form.barcode} onChange={e => change('barcode', e.target.value.toUpperCase())} placeholder="ES-000001 or manufacturer code" disabled={Boolean(initialProduct)} /></label><label>QR code<input value={form.qrCode} onChange={e => change('qrCode', e.target.value)} placeholder="Defaults to barcode" /></label><label>Brand<input value={form.brand} onChange={e => change('brand', e.target.value)} placeholder="Brand name" /></label><label className="span-two">Description<textarea value={form.description} onChange={e => change('description', e.target.value)} rows="3" placeholder="Product description" /></label></div></div><div className="form-section"><h3>Inventory information</h3><div className="form-grid"><label>Category<select value={form.category} onChange={e => change('category', e.target.value)}><option value="">Select category</option>{categories.map(item => <option key={item._id} value={item._id}>{item.name}</option>)}</select></label><label>Supplier<select value={form.supplier} onChange={e => change('supplier', e.target.value)}><option value="">Select supplier</option>{suppliers.map(item => <option key={item._id} value={item._id}>{item.name}</option>)}</select></label><label>Unit type<select value={form.unitType} onChange={e => change('unitType', e.target.value)}><option>piece</option><option>case</option><option>box</option><option>pack</option><option>kg</option><option>liter</option><option>sack</option></select></label><label>Branch / warehouse<input value={form.branch} onChange={e => change('branch', e.target.value)} /></label><label>Initial quantity<input type="number" min="0" step="1" value={form.currentStock} onChange={e => change('currentStock', e.target.value)} /></label><label>Reorder level<input type="number" min="0" step="1" value={form.reorderLevel} onChange={e => change('reorderLevel', e.target.value)} /></label><label>Expiration date <span className="field-hint">Optional</span><input type="date" value={form.expirationDate} onChange={e => change('expirationDate', e.target.value)} /></label></div></div><div className="form-section"><h3>Inventory valuation</h3><div className="form-grid"><label>Cost price<input type="number" min="0" step="0.01" value={form.costPrice} onChange={e => change('costPrice', e.target.value)} /></label><div className="valuation-preview"><span>Calculated inventory value</span><strong>₱{(Number(form.currentStock || 0) * Number(form.costPrice || 0)).toLocaleString('en-PH', { minimumFractionDigits: 2 })}</strong></div></div></div><div className="form-actions"><button type="button" className="secondary-btn" onClick={() => window.history.back()}>Cancel</button><button className="primary-btn" disabled={busy}>{busy ? 'Saving...' : initialProduct ? 'Update Product' : 'Register Product'}</button></div></form>;
}